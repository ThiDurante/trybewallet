// Coloque aqui suas actions
export const loginEvent = (email) => ({
  type: 'loginEvent',
  payload: email,
});

// placeholder for esLint
export const loginEvent2 = (email) => ({
  type: 'loginEvent',
  payload: email,
});
